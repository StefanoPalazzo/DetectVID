server {
    listen 80;
    server_name localhost;

    resolver 127.0.0.11 valid=30s ipv6=off;

    # Archivos estáticos de React
    location / {
        root /usr/share/nginx/html;
        index index.html index.htm;
        try_files $uri $uri/ /index.html;
    }

    # Proxy reverso para la API de ML (PyTorch/FastAPI)
    # Ejemplo: Fetch a /api/ml/predict -> FastAPI:8000/predict
    location /api/ml/ {
        set $ml_upstream http://ml:8000;
        proxy_pass $ml_upstream;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        # Para subir imágenes grandes
        client_max_body_size 20M;
    }

    # Proxy for locally persisted uploaded images
    location /uploads/ {
        set $backend_uploads_upstream http://backend:3001/uploads/;
        proxy_pass $backend_uploads_upstream;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        client_max_body_size 20M;
    }

    # Proxy reverso para el Backend (Express)
    # Ejemplo: Fetch a /api/users -> Express:3000/api/users
    location /api/ {
        set $backend_upstream http://backend:3001;
        proxy_pass $backend_upstream;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        client_max_body_size 20M;
    }
}
